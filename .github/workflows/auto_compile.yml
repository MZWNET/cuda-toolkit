# `dist/index.js` is a special file in Actions.
# When you reference an action with `uses:` in a workflow,
# `index.js` is the code that will run.
# For our project, we generate this file through a build process from other source files.
# We need to make sure the checked-in `index.js` actually matches what we expect it to be.
name: Auto Compile dist/index.js

on:
  workflow_dispatch:
    inputs:
      force_compile:
        description: Force compile the dist/index.js file
        required: false
        type: boolean
  push:
    branches:
      - master
      - compile

jobs:
  auto_compile:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master

      - name: Set Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          check-latest: true
          package-manager-cache: true

      - name: Install dependencies
        run: |
          pnpm i --frozen-lockfile

      - name: Rebuild the dist/ directory
        run: pnpm run package

      - name: Upload dist/index.mjs to GitHub Repository
        run: |
          git clone https://github-action:${{ secrets.GITHUB_TOKEN }}@github.com/MZWNET/cuda-toolkit.git -b master cache
          rm -rf cache/dist/index.mjs
          cp dist/index.mjs cache/dist/index.mjs
          cp dist/index.mjs.map cache/dist/index.mjs.map
          cd cache
          if ! git diff --quiet HEAD -- dist/index.mjs; then
            echo "dist/index.mjs has changes."
            git config --local user.email "actions@github.com"
            git config --local user.name "GitHub Actions Bot"
            git status
            git add dist/index.mjs dist/index.mjs.map
            git commit -m "feat: update dist/index.mjs"
            git push origin master
          else
            echo "No changes in dist/index.mjs."
          fi
